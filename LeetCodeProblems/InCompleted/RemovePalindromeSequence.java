package OtherProblems.LeetCodeProblems.InCompleted;

//This problem is incomplete.

public class RemovePalindromeSequence {
    public static void main(String[] args) {
        String s = "ababa";
        int ans = removePalindromeSequence(s);
        System.out.println(ans);
    }

    static int removePalindromeSequence(String s) {
        if (s == "" || s == null)
            return 0;

        return 5;
    }
}
